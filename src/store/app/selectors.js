export const getStats = (state) => state.app.stats;

export const getTypes = (state) => state.app.types;

export const getIsLoading = (state) => state.app.isLoading;

export const getError = (state) => state.app.error;
